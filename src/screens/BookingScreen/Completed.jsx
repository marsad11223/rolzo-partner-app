import React, { useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  useWindowDimensions,
  TextInput,
} from 'react-native';

const Completed = () => {
  return <View style={{ flex: 1 }} />;
};

export default Completed;
